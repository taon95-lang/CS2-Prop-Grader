{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "node": "lts",
    "pnpm": "latest"
  },
  "installCommand": "uv sync --locked --no-dev --no-install-project && pnpm install --frozen-lockfile"
}
